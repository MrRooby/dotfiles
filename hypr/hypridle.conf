listener {
    timeout = 300
    on-timeout = hyprlock                   
    on-resume = brightnessctl --restore    
}

listener {
    timeout = 360
    on-timeout = swaymsg "output * power off"     
    on-resume = swaymsg "output * power on"      
}
